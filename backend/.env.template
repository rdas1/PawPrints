DB_PATH=./pawprints.db
PORT=5000
SEED_ON_BOOT=true

# Copy .env.template to .env and edit values as needed.
# Use the following command to copy the template to a new file during deployment:
# cp .env.template .env